https://www.youtube.com/watch?v=JSpC7Cz64h0&list=PLdo4fOcmZ0oVxKLQCHpiUWun7vlJJvUiN&index=1

3. Strings
	string interpolation with $"value is : {var_name}" string formatting syntax
	string.Trim()
	string.TrimStart()
	string.TrimEnd()
	string.Length
	string.Contains("search_str")
	string.Replace("str", "replace_with")

4. Strings
	string.ToUpper() // returns new string
	string.ToLower() // returns new string
	string.StartsWith("prefix")
	string.EndsWith("suffix")

5. integers
	int.MaxValue
	int.MinValue

	operator precidence and associavity

	sbyte => System.SByte
	short => System.Int16
	int => System.Int32
	long => System.Int64
	
6. integers division and double
	integer division result is integer
	singned integers to into -ve range after max value, typical singed integer behavior

	double : decimals and range is bigger
	decimal dm = 1.23M

7. decimals
	range is smaller than double but precision is greater	
	float, 4 bytes, System.Single, -45,+38.6-9
	double, 8 bytes, System.Double	-324,+308.15-17
	decimal, 4 bytes, System.Decimal -28,+28.28-29

8-11: Not much

12:	List
	var l = new System.Collection.Generic.List{"rahul", "well", "good"};
	l.Add("hello");
	l.Remove("well");
	l.IndexOf("well"); // returns -1, its not present
	l.IndexOf("hello"); // returns 2, its not present
	l.sort(); // inplace sort


	

