member fucntions = members methods, properties, indexers
const = read only
cstror, dstctor = constructor, finalizer
default cstor = parameter less constrctor
friend functions = extended methods

template type initialization is called as = they are instance of System.Nullable<T>;

pure virual : abstract
rethrow = plain "throw;" will rethrow the exception it received

ranges from python

implicit and explict conversion have function signature types

ref paramters are not allowed in lambda
	because local ref parameters have certain life
	change in ref paramters must reflect outside of lambda
	this are incompatible properties

sealed is like final
