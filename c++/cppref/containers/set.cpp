Set
	sorted container

	set<T, Compare = std::less<T>, Allocator = std::allocator<T>>

	insert_range

	insert (with hint, range, begin/end)
	emplace_hint
	 
	swap : optimized no move, copy, or swap operations on individual elements.

	extract: (key, pos itr, ) returns node_handle

	contains
	  
